import styled from 'styled-components';

export const Container = styled.ul`
  grid-area: SIDENAV;

  background: #000000;

  box-shadow: var(--elevation-layout);
  z-index: 2;
  display: block;

  overflow: hidden;
`;
